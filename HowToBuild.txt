To only compile you can just open solution in Visual Studio.

To perform full build with tests:
-install ruby
-run "gem install zip"
-run "gem install rake"
-run "gem install albacore"
-run "rake" from project root
